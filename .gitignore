# --- Python Specific ---
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
build/
dist/

# --- Streamlit Secrets (ความลับของ Firebase ห้ามหลุด!) ---
.streamlit/secrets.toml

# --- Database & Guidelines Data (ไฟล์ที่คุณไม่อยากอัปขึ้น) ---
# ป้องกันการอัปโหลดไฟล์ CSV ทั้งหมด
*.csv
# ป้องกันไฟล์ JSON ที่อาจเป็น Service Account Key
*.json
# ป้องกันไฟล์ Excel หากมี
*.xlsx

# --- Upload Scripts ---
# หากต้องการข้ามไฟล์สคริปต์ที่ใช้สำหรับอัปโหลดข้อมูลโดยเฉพาะ
upload_guidelines.py

# --- System Files ---
.DS_Store
Thumbs.db